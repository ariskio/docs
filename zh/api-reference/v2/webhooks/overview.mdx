---
title: "接收与验签（V2）"
description: "了解 Arisk V2 Webhook 的基本原理以及如何验证推送消息的安全性。"
---

Arisk V2 通过 Webhook 向您配置的服务器发送实时 HTTP POST 请求，通知监控地址的风险等级变动或风险交易。

## 安全性验证

为确保消息来源的真实性和完整性，Arisk 发送的每个 Webhook 请求都包含数字签名。我们强烈建议您在处理请求前进行验证。

### HTTP 请求头

每个 Webhook 请求都会包含以下关键 Header：

| Header | 说明 | 示例 |
| :--- | :--- | :--- |
| **X-Arisk-Signature** | RSA-PSS 数字签名（Base64 编码） | `aGVsbG8gd29ybGQ=...` |
| **Authorization** | Bearer Token（仅在配置了 `auth_key` 时存在） | `Bearer your-secret-token` |

### 验证步骤

1.  **验证签名（必须）**
    *   **算法**: RSA-PSS with SHA-256
    *   **签名内容**: 请求 Body 的原始 JSON 字符串
    *   **公钥**: 使用下方的 Arisk RSA 公钥验证 `X-Arisk-Signature` 中的签名。

<CodeGroup>
```text 公钥 (PEM)
-----BEGIN PUBLIC KEY-----

awIDAQAB
-----END PUBLIC KEY-----
```
</CodeGroup>

2.  **验证 Authorization（可选）**
    如果您在 Webhook 配置中设置了 `auth_key`，Arisk 会在 Header 中添加：
    `Authorization: Bearer <您的 auth_key>`

---

## 接收与处理流程

1. 保留请求体原始内容，并按上方说明验证签名。不要将解析后重新序列化的 JSON 用于验签。
2. 配置了 `auth_key` 时，核对 `Authorization` 的值。
3. 按 `type` 识别事件，使用 `alert.id` 做幂等检查，避免重复处理。
4. 建议先将已验证的事件可靠地保存或写入持久化队列，再尽快返回 HTTP `200`。响应体可为空，服务端不解析其内容。
5. 在后台执行耗时业务逻辑，避免占用接收请求。不要把未通过验证的消息当作有效事件处理。

网络波动或重试可能带来重复通知。接收服务和后台消费者都应避免对同一个告警重复产生业务效果。

## 事件参考

- [地址风险等级变动](/zh/api-reference/v2/webhooks/address-risk-change)
- [交易检测](/zh/api-reference/v2/webhooks/transaction-detection)
- [地址监控接入](/zh/guides/v2/monitoring)
