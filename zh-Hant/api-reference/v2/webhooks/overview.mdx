---
title: "接收與驗簽（V2）"
description: "瞭解 Arisk V2 Webhook 的基本原理以及如何驗證推送訊息的安全性。"
---

Arisk V2 透過 Webhook 向您配置的伺服器傳送實時 HTTP POST 請求，通知監控地址的風險等級變動或風險交易。

## 安全性驗證

為確保訊息來源的真實性和完整性，Arisk 傳送的每個 Webhook 請求都包含數字簽名。我們強烈建議您在處理請求前進行驗證。

### HTTP 請求頭

每個 Webhook 請求都會包含以下關鍵 Header：

| Header | 說明 | 示例 |
| :--- | :--- | :--- |
| **X-Arisk-Signature** | RSA-PSS 數字簽名（Base64 編碼） | `aGVsbG8gd29ybGQ=...` |
| **Authorization** | Bearer Token（僅在配置了 `auth_key` 時存在） | `Bearer your-secret-token` |

### 驗證步驟

1.  **驗證簽名（必須）**
    *   **演算法**: RSA-PSS with SHA-256
    *   **簽名內容**: 請求 Body 的原始 JSON 字串
    *   **公鑰**: 使用下方的 Arisk RSA 公鑰驗證 `X-Arisk-Signature` 中的簽名。

<CodeGroup>
```text 公鑰 (PEM)
-----BEGIN PUBLIC KEY-----

awIDAQAB
-----END PUBLIC KEY-----
```
</CodeGroup>

2.  **驗證 Authorization（可選）**
    如果您在 Webhook 配置中設定了 `auth_key`，Arisk 會在 Header 中新增：
    `Authorization: Bearer <您的 auth_key>`

---

## 接收與處理流程

1. 保留請求本文的原始內容，並依上方說明驗證簽名。不要將解析後重新序列化的 JSON 用於驗簽。
2. 設定了 `auth_key` 時，核對 `Authorization` 的值。
3. 依 `type` 識別事件，使用 `alert.id` 進行冪等檢查，避免重複處理。
4. 建議先將已驗證的事件可靠地儲存或寫入持久化佇列，再儘快返回 HTTP `200`。回應本文可為空，伺服器不解析其內容。
5. 在背景執行耗時的業務邏輯，避免占用接收請求。不要將未通過驗證的訊息當作有效事件處理。

網路波動或重試可能帶來重複通知。接收服務和背景消費者都應避免對同一個告警重複產生業務效果。

## 事件參考

- [地址風險等級變動](/zh-Hant/api-reference/v2/webhooks/address-risk-change)
- [交易檢測](/zh-Hant/api-reference/v2/webhooks/transaction-detection)
- [地址監控接入](/zh-Hant/guides/v2/monitoring)
